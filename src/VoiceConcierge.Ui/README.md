# VoiceConcierge.Ui

The **React admin UI** (PRD bonus — Epics 4-7). Static client bundle; **no Node
runtime service** (Vite/Node is build-time tooling only).

Built here by the Sonnet workstream per
`_bmad-output/implementation-artifacts/SONNET-IMPLEMENTATION-PROMPT.md`:
React + Vite + TypeScript, calling the `VoiceConcierge.Api` REST endpoints,
embedding the LiveKit playground (`@livekit/components-react`).

Naming/layout mirrors `/home/andriiyefymenko/Work/seqaro` conventions
(`<Product>.<Component>`); compose service name: `voiceconcierge-ui`.

> Placeholder — implementation pending (Stories 4-1..7-2, Sonnet-owned).
